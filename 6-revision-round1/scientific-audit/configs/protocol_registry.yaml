# Registro canonico de protocolos (fonte da Tabela 23 do artigo).
# Editar AQUI e regenerar a tabela; nao editar a tabela a mao.
experiments:
  main_factorial:   {tables: [7,8,9,10,12], seeds: 42-53, models: frozen TERA pipeline per seed, decision: "episode = mean last k=6 frames vs thr_ep(VAL, F1 max s.t. FR<=0.05); TTD theta=0.10 m=3", budget: none, ece: ECE_pipe}
  borderline_mov6:  {tables: [13], seeds: [42], models: frozen factorial, decision: "p_bar_max descriptor @ theta=0.10", budget: none, ece: null}
  posthoc_calibration: {tables: [14], seeds: 42-45, models: frozen Baseline + calibrators fitted on VAL, decision: pipeline rules; MHEG thresholds recalibrated per variant, budget: none, ece: ECE_cal}
  teacher_ablation: {tables: [15], seeds: 42-45, models: retrained simplified (60 ep, no scheduler), decision: internal only, budget: none, ece: null, comparable_to_main: false}
  mechanism_ablation: {tables: [11], seeds: 42-53, models: all 5 arms retrained under run_v29, decision: run_v29 rules (tau_H=0.95 bits), budget: none, ece: ECE_v29, comparable_to_main: false}
  stress_campaign:  {tables: [16,17,18], seeds: 42-45, models: signal-level simulation (H normalized [0,1] by design), decision: FS@onset/coverage/miss, budget: compute-rate matched +-2%, ece: null}
  gating_baselines: {tables: [19], seeds: 42-45, models: frozen run_v29 trajectories, decision: "label onset y>0.35; theta=0.10; m=3; oracle grid on test (declared)", budget: matched suppression, ece: null, extras: [periodic_refresh R in [2,4,8]]}
  skiprnn:          {tables: [20], seeds: 42-45, models: Skip-RNN trained end-to-end; AF-TOI frozen, decision: harmonized (label onset, theta=0.10, m=3) + PreOcc/NormOcc, budget: matched suppression, ece: null}
  temporal_warp:    {tables: [21], figures: [14], seeds: 42-53, models: frozen 12-seed models+thresholds, decision: label-onset detection on warped episodes (warp scale; not comparable to Table 8), budget: bisection-matched compute <=2%, ece: null}
  deployment:       {tables: [22], seeds: null, models: deployed causal LSTM (student), decision: analytical counts + reference-GPU latency (1 MAC = 2 FLOPs), budget: none, ece: null}
